want to inject at SimpleOptions.class:
line 138

->
For gamma > 1, callbacks.validate returns defaultValue
->
use mixin to force T object = value, sidestepping callbacks.validate


inject at line 143?
	-> if this.equals(getGamma()){ 
			this.value = value;
		else{
			this.value = object;
		}